# 国际化支持实施计划

## 当前状态

### ✅ 已完成
1. **依赖安装** - `next-intl@^3.21.0` 已添加到 package.json
2. **路由配置** - `src/i18n/routing.ts` 已创建，配置了中英文双语支持

### 🔄 进行中
需要完成以下工作才能实现完整的国际化支持。

---

## 技术选型：next-intl

**选择理由：**
1. Next.js 官方推荐的国际化解决方案
2. 原生支持 Next.js 16 App Router
3. 提供服务端和客户端组件的完整支持
4. 内置路由国际化（URL 路径带语言前缀）
5. 支持 SSR/SSG 和动态渲染
6. 良好的 TypeScript 支持

---

## 实施步骤

### 第一阶段：完成基础设施（待执行）

#### 1.1 创建服务端配置 `src/i18n/request.ts`
```typescript
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;

  if (!locale || !routing.locales.includes(locale as any)) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default
  };
});
```

#### 1.2 创建翻译文件目录结构
```
src/
├── messages/
│   ├── zh-CN.json    # 中文翻译
│   └── en-US.json    # 英文翻译
```

#### 1.3 创建 middleware.ts（项目根目录）
```typescript
import createMiddleware from 'next-intl/middleware';
import { routing } from './src/i18n/routing';

export default createMiddleware(routing);

export const config = {
  matcher: ['/', '/(zh-CN|en-US)/:path*']
};
```

#### 1.4 修改 next.config.ts
```typescript
import createNextIntlPlugin from 'next-intl/plugin';
import type { NextConfig } from "next";

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

const nextConfig: NextConfig = {
  // 现有配置
};

export default withNextIntl(nextConfig);
```

---

### 第二阶段：翻译文件创建

#### 2.1 中文翻译 `src/messages/zh-CN.json`
```json
{
  "common": {
    "loading": "加载中...",
    "save": "保存",
    "cancel": "取消",
    "confirm": "确认",
    "delete": "删除",
    "edit": "编辑",
    "create": "创建",
    "search": "搜索"
  },
  "auth": {
    "login": {
      "title": "登录",
      "description": "输入您的账号信息登录系统",
      "username": "用户名",
      "usernamePlaceholder": "请输入用户名",
      "password": "密码",
      "passwordPlaceholder": "请输入密码",
      "submit": "登录",
      "noAccount": "还没有账号？",
      "register": "立即注册",
      "success": "登录成功",
      "error": "登录失败",
      "errorDescription": "请检查用户名和密码"
    },
    "register": {
      "title": "注册",
      "description": "创建新账号",
      "username": "用户名",
      "email": "邮箱",
      "password": "密码",
      "confirmPassword": "确认密码",
      "submit": "注册",
      "hasAccount": "已有账号？",
      "login": "立即登录"
    }
  },
  "nav": {
    "chat": "聊天",
    "agent": "Agent",
    "workspace": "工作区",
    "tasks": "任务",
    "skills": "Skills",
    "settings": "设置"
  },
  "chat": {
    "searchPlaceholder": "搜索对话",
    "noConversations": "暂无对话",
    "startNew": "开始一个新的对话吧",
    "newChat": "新建对话",
    "selectConversation": "选择一个对话",
    "selectOrNew": "从左侧选择一个对话开始聊天，或创建新对话"
  },
  "settings": {
    "title": "设置",
    "profile": {
      "title": "个人资料",
      "description": "管理您的账号信息",
      "username": "用户名",
      "email": "邮箱",
      "save": "保存更改"
    },
    "appearance": {
      "title": "外观",
      "description": "自定义应用外观",
      "theme": "主题",
      "themeDescription": "选择浅色或深色主题"
    },
    "api": {
      "title": "API 配置",
      "description": "配置 LLM API Key",
      "key": "API Key",
      "baseUrl": "API Base URL",
      "save": "保存配置"
    },
    "notifications": {
      "title": "通知",
      "description": "管理通知设置",
      "desktop": "桌面通知",
      "desktopDescription": "接收桌面推送通知",
      "sound": "消息声音",
      "soundDescription": "收到新消息时播放声音"
    }
  },
  "metadata": {
    "title": "AloneChat",
    "description": "AI Agent 协作平台 - 实时聊天 × 智能Agent × RAG检索 × 多Agent编排"
  }
}
```

#### 2.2 英文翻译 `src/messages/en-US.json`
```json
{
  "common": {
    "loading": "Loading...",
    "save": "Save",
    "cancel": "Cancel",
    "confirm": "Confirm",
    "delete": "Delete",
    "edit": "Edit",
    "create": "Create",
    "search": "Search"
  },
  "auth": {
    "login": {
      "title": "Login",
      "description": "Enter your credentials to login",
      "username": "Username",
      "usernamePlaceholder": "Enter username",
      "password": "Password",
      "passwordPlaceholder": "Enter password",
      "submit": "Login",
      "noAccount": "Don't have an account?",
      "register": "Sign up",
      "success": "Login successful",
      "error": "Login failed",
      "errorDescription": "Please check your username and password"
    },
    "register": {
      "title": "Register",
      "description": "Create a new account",
      "username": "Username",
      "email": "Email",
      "password": "Password",
      "confirmPassword": "Confirm Password",
      "submit": "Register",
      "hasAccount": "Already have an account?",
      "login": "Sign in"
    }
  },
  "nav": {
    "chat": "Chat",
    "agent": "Agent",
    "workspace": "Workspace",
    "tasks": "Tasks",
    "skills": "Skills",
    "settings": "Settings"
  },
  "chat": {
    "searchPlaceholder": "Search conversations",
    "noConversations": "No conversations",
    "startNew": "Start a new conversation",
    "newChat": "New Chat",
    "selectConversation": "Select a conversation",
    "selectOrNew": "Select a conversation from the left or create a new one"
  },
  "settings": {
    "title": "Settings",
    "profile": {
      "title": "Profile",
      "description": "Manage your account information",
      "username": "Username",
      "email": "Email",
      "save": "Save Changes"
    },
    "appearance": {
      "title": "Appearance",
      "description": "Customize app appearance",
      "theme": "Theme",
      "themeDescription": "Choose between light and dark theme"
    },
    "api": {
      "title": "API Configuration",
      "description": "Configure LLM API Key",
      "key": "API Key",
      "baseUrl": "API Base URL",
      "save": "Save Configuration"
    },
    "notifications": {
      "title": "Notifications",
      "description": "Manage notification settings",
      "desktop": "Desktop Notifications",
      "desktopDescription": "Receive desktop push notifications",
      "sound": "Message Sound",
      "soundDescription": "Play sound when receiving new messages"
    }
  },
  "metadata": {
    "title": "AloneChat",
    "description": "AI Agent Collaboration Platform - Real-time Chat × Intelligent Agent × RAG Retrieval × Multi-Agent Orchestration"
  }
}
```

---

### 第三阶段：修改根布局

修改 `src/app/layout.tsx`，添加 NextIntlClientProvider：

```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { Toaster } from "@/components/ui/sonner";
import { TooltipProvider } from "@/components/ui/tooltip";
import { QueryProvider } from "@/lib/query-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AloneChat",
  description: "AI Agent 协作平台 - 实时聊天 × 智能Agent × RAG检索 × 多Agent编排",
  keywords: ["AI", "Agent", "Chat", "RAG", "Multi-Agent"],
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const messages = await getMessages();
  
  return (
    <html
      lang="zh-CN"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <NextIntlClientProvider messages={messages}>
          <QueryProvider>
            <TooltipProvider>
              {children}
              <Toaster richColors position="top-right" />
            </TooltipProvider>
          </QueryProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

---

### 第四阶段：创建语言切换组件

创建 `src/components/common/language-switcher.tsx`：

```typescript
'use client'

import { useLocale } from 'next-intl';
import { useRouter, usePathname } from '@/i18n/routing';
import { Globe } from 'lucide-react';
import { Button } from '@/components/ui/button';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu';

const languages = [
  { code: 'zh-CN', name: '简体中文' },
  { code: 'en-US', name: 'English' },
];

export function LanguageSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();
  
  const handleLanguageChange = (newLocale: string) => {
    router.replace(pathname, { locale: newLocale });
  };
  
  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon">
          <Globe className="h-4 w-4" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        {languages.map((lang) => (
          <DropdownMenuItem
            key={lang.code}
            onClick={() => handleLanguageChange(lang.code)}
            className={locale === lang.code ? 'bg-accent' : ''}
          >
            {lang.name}
          </DropdownMenuItem>
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

---

### 第五阶段：组件迁移

#### 需要迁移的文件

| 文件 | 优先级 | 说明 |
|------|--------|------|
| `src/app/(auth)/login/page.tsx` | 高 | 登录页面 |
| `src/app/(auth)/register/page.tsx` | 高 | 注册页面 |
| `src/components/layout/sidebar.tsx` | 高 | 侧边栏导航 |
| `src/app/(dashboard)/settings/page.tsx` | 高 | 设置页面 |
| `src/app/(dashboard)/chat/page.tsx` | 中 | 聊天页面 |
| `src/app/(dashboard)/agent/page.tsx` | 中 | Agent页面 |
| `src/app/(dashboard)/workspace/page.tsx` | 中 | 工作区页面 |
| `src/app/(dashboard)/tasks/page.tsx` | 中 | 任务页面 |
| `src/app/(dashboard)/skills/page.tsx` | 中 | Skills页面 |
| `src/components/layout/header.tsx` | 中 | 头部组件 |
| `src/components/layout/mobile-nav.tsx` | 中 | 移动端导航 |

#### 迁移示例

**登录页面迁移：**
```typescript
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import { useTranslations } from 'next-intl'
import { Loader2 } from 'lucide-react'
// ... 其他导入

export default function LoginPage() {
  const t = useTranslations('auth.login')
  // ... 其他逻辑
  
  return (
    <Card className="w-full max-w-md">
      <CardHeader className="text-center">
        <CardTitle className="text-2xl">{t('title')}</CardTitle>
        <CardDescription>{t('description')}</CardDescription>
      </CardHeader>
      <form onSubmit={handleSubmit}>
        <CardContent className="space-y-4">
          <div className="space-y-2">
            <Label htmlFor="username">{t('username')}</Label>
            <Input
              id="username"
              type="text"
              placeholder={t('usernamePlaceholder')}
              // ...
            />
          </div>
          {/* ... */}
        </CardContent>
        <CardFooter className="flex flex-col gap-4">
          <Button type="submit" className="w-full" disabled={isLoading}>
            {isLoading && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}
            {t('submit')}
          </Button>
          <p className="text-sm text-muted-foreground">
            {t('noAccount')}{' '}
            <Link href="/register" className="text-primary hover:underline">
              {t('register')}
            </Link>
          </p>
        </CardFooter>
      </form>
    </Card>
  )
}
```

**侧边栏迁移：**
```typescript
'use client'

import { useTranslations } from 'next-intl'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { MessageSquare, Bot, FolderOpen, ListTodo, Puzzle, Settings } from 'lucide-react'
// ... 其他导入

export function Sidebar() {
  const t = useTranslations('nav')
  const pathname = usePathname()
  
  const navItems = [
    { href: '/chat', label: t('chat'), icon: MessageSquare },
    { href: '/agent', label: t('agent'), icon: Bot },
    { href: '/workspace', label: t('workspace'), icon: FolderOpen },
    { href: '/tasks', label: t('tasks'), icon: ListTodo },
    { href: '/skills', label: t('skills'), icon: Puzzle },
  ]
  
  // ... 渲染逻辑
}
```

---

### 第六阶段：测试验证

#### 功能测试清单
- [ ] 语言切换正常工作
- [ ] URL 路径正确包含语言前缀 (`/zh-CN/...`, `/en-US/...`)
- [ ] 刷新页面后语言保持
- [ ] 所有页面文本已翻译
- [ ] 表单验证消息已翻译
- [ ] 错误提示已翻译
- [ ] SEO 元数据已翻译

#### 测试命令
```bash
# 安装依赖
pnpm install

# 开发模式测试
pnpm dev

# 构建测试
pnpm build

# 类型检查
pnpm lint
```

---

## 文件变更清单

### 新增文件
| 文件路径 | 说明 |
|----------|------|
| `src/i18n/request.ts` | 服务端 i18n 配置 |
| `src/messages/zh-CN.json` | 中文翻译文件 |
| `src/messages/en-US.json` | 英文翻译文件 |
| `middleware.ts` | 语言检测中间件（项目根目录） |
| `src/components/common/language-switcher.tsx` | 语言切换组件 |

### 修改文件
| 文件路径 | 修改内容 |
|----------|----------|
| `next.config.ts` | 集成 next-intl 插件 |
| `src/app/layout.tsx` | 添加 NextIntlClientProvider |
| `src/app/(auth)/login/page.tsx` | 使用翻译函数 |
| `src/app/(auth)/register/page.tsx` | 使用翻译函数 |
| `src/app/(dashboard)/chat/page.tsx` | 使用翻译函数 |
| `src/app/(dashboard)/settings/page.tsx` | 使用翻译函数 |
| `src/components/layout/sidebar.tsx` | 使用翻译函数 |
| `src/components/layout/header.tsx` | 添加语言切换组件 |
| 所有其他页面和组件 | 替换硬编码文本 |

---

## 预估工作量

| 阶段 | 预估时间 | 状态 |
|------|----------|------|
| 基础设施搭建 | 30 分钟 | 待执行 |
| 翻译文件创建 | 30 分钟 | 待执行 |
| 根布局修改 | 10 分钟 | 待执行 |
| 语言切换组件 | 15 分钟 | 待执行 |
| 组件迁移 | 2-3 小时 | 待执行 |
| 测试验证 | 30 分钟 | 待执行 |
| **总计** | **约 4 小时** | - |

---

## 注意事项

1. **URL 结构变化**：所有 URL 将带有语言前缀，如 `/zh-CN/chat`、`/en-US/chat`
2. **向后兼容**：middleware 会自动将 `/chat` 重定向到 `/zh-CN/chat`
3. **SEO 友好**：每个语言版本都有独立的 URL
4. **性能考虑**：翻译文件按语言加载，不会影响首屏性能
5. **类型安全**：利用 TypeScript 确保翻译键的正确性

---

## 参考资料

- [next-intl 官方文档](https://next-intl-docs.vercel.app/)
- [Next.js 国际化指南](https://nextjs.org/docs/app/building-your-application/routing/internationalization)
- [ICU Message Format](https://unicode-org.github.io/icu/userguide/format_parse/messages/)
